# Pattern Lock Simulator

A small interactive web project that recreates the Android-style pattern lock grid and simulates all valid unlock patterns.
Draw patterns by dragging across the dots, or automatically visualize every possible valid pattern using the Simulate button.
